apiVersion: v1
kind: Service
metadata:
  name: {{ include "paperclip.fullname" . }}
  labels:
    {{- include "paperclip.labels" . | nindent 4 }}
  {{- with .Values.paperclip.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.paperclip.service.type }}
  ports:
    - port: {{ .Values.paperclip.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paperclip.selectorLabels" . | nindent 4 }}
